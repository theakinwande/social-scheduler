# X API Credentials
# Get these from https://developer.x.com/en/portal/dashboard
#
# Required for POSTING (OAuth 1.0a User Context):
#   1. Go to your app > Keys and tokens
#   2. Under "Consumer Keys" - copy API Key and API Secret
#   3. Under "Authentication Tokens" > "Access Token and Secret" - click Generate
#      (This creates tokens for YOUR account - the app owner)
#
X_API_KEY=your_consumer_api_key
X_API_SECRET=your_consumer_api_secret
X_ACCESS_TOKEN=your_access_token
X_ACCESS_SECRET=your_access_token_secret

# Server Config
PORT=3000
NODE_ENV=development